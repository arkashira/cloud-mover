[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"

[tool.poetry]
name = "cloud-mover"
version = "1.0.0"
description = "Cloud Mover"

[tool.poetry.dependencies]
python = "^3.8"
